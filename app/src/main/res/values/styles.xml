<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    
    <style name="HeaderText">
        <item name="android:textSize">@dimen/text_big</item>
    </style>
    
    <style name="HeaderTextBlue" parent="HeaderText">
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="HeaderTextBlueC" parent="HeaderTextBlue">
        <item name="android:gravity">center</item>
        <item name="android:layout_gravity">center</item>
    </style>

    <style name="HeaderTextTop" parent="HeaderTextBlueC">
        <item name="android:textColor">@color/colorWhite</item>
    </style>

    <style name="UsualTextBlue">
        <item name="android:textSize">@dimen/text_standart</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
    </style>

    <style name="HeaderTextBluePad" parent="HeaderTextBlue">
        <item name="android:padding">@dimen/padding_med</item>
    </style>

</resources>
